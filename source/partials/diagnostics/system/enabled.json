{
	"compilerOptions": {
		"extendedDiagnostics": true,
		"generateTrace": "${configDir}/artifacts/typescript/trace/",
		"generateCpuProfile": "${configDir}/artifacts/typescript/profile.cpuprofile"
	}
}